<Page
    x:Class="_1809_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_1809_UWP"
    xmlns:local1="using:Shared_Code"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="54" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <local:NavbarControl Background="Black" HorizontalAlignment="Center" Width="57"/>

        <ScrollViewer Grid.Column="1" Margin="10,10,10,10">
            <StackPanel>
                <ItemsControl ItemsSource="{x:Bind Cards}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local1:Card">
                            <StackPanel>
                                <Border Padding="10" Margin="0, 10" HorizontalAlignment="Stretch">
                                    <Border.Background>
                                        <AcrylicBrush BackgroundSource="HostBackdrop" TintColor="#99000000" TintOpacity="0.6" />
                                    </Border.Background>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{x:Bind CardName}" TextAlignment="Center" Foreground="White" Grid.Row="0"/>
                                        <Button Content="View Card" Click="ViewCard_Click" CommandParameter="{Binding}" Margin="0,10" Background="Transparent" HorizontalAlignment="Stretch" Grid.Row="1"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
