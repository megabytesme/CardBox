<Page
    x:Class="_1809_UWP.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_1809_UWP"
    xmlns:local1="using:Shared_Code"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer Margin="10,10,10,10">
            <StackPanel>
                <ItemsControl ItemsSource="{x:Bind FilteredCards}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local1:Card">
                            <Grid>
                                <Rectangle Fill="Black" Opacity="0.3" RadiusX="8" RadiusY="8" Margin="10"/>
                                <Border Padding="10" Margin="0, 10" HorizontalAlignment="Stretch" CornerRadius="8">
                                    <Border.Background>
                                        <AcrylicBrush BackgroundSource="Backdrop" TintColor="#99000000" TintOpacity="0" FallbackColor="#2D2D2D" />
                                    </Border.Background>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{x:Bind CardName}" TextAlignment="Center" Foreground="White" Grid.Row="0"/>
                                        <Button Content="View Card" Click="ViewCard_Click" CommandParameter="{Binding}" Margin="0,10" Foreground="White" HorizontalAlignment="Stretch" Grid.Row="1" CornerRadius="4"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
