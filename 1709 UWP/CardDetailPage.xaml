<Page
    x:Class="_1709_UWP.CardDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_1709_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <ScrollViewer>
            <StackPanel Spacing="20">
                <Border BorderBrush="LightGray" BorderThickness="1" Padding="20">
                    <Border.Background>
                        <AcrylicBrush BackgroundSource="HostBackdrop" TintColor="Transparent" TintOpacity="0.6" FallbackColor="Gray"/>
                    </Border.Background>
                    <StackPanel Spacing="10">
                        <TextBlock Text="{Binding CardName}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" Foreground="White"/>
                        <TextBlock Text="{Binding CardNickname}" FontSize="20" Margin="0,0,0,10" Foreground="LightGray"/>
                        <Image x:Name="barcodeImage" Height="200" Width="200" Stretch="Fill"/>
                        <TextBlock Text="{Binding CardNumber}" FontSize="20" Margin="0,-10" HorizontalAlignment="Center" Foreground="LightGray"/>
                    </StackPanel>
                </Border>

                <Border BorderBrush="LightGray" BorderThickness="1" Padding="20">
                    <Border.Background>
                        <AcrylicBrush BackgroundSource="HostBackdrop" TintColor="Transparent" TintOpacity="0.6" FallbackColor="Gray"/>
                    </Border.Background>
                    <StackPanel Spacing="10">
                        <TextBlock Text="Nearby Locations" FontSize="20" Margin="0,0,0,10" Foreground="LightGray"/>
                        <ProgressRing x:Name="loadingProgressRing" IsActive="False" Visibility="Collapsed" Width="40" Height="40" HorizontalAlignment="Center"/>
                        <ListView x:Name="locationsListView" Height="200" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontSize="16" Foreground="White" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Distance}" FontSize="14" Foreground="LightGray" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Address}" FontSize="14" Foreground="LightGray" Grid.Row="1" Grid.Column="0" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                            <Button Click="NavigateToLocation_Click" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <Button.Content>
                                                    <FontIcon Glyph="&#xE816;" />
                                                </Button.Content>
                                            </Button>
                                        </Grid>
                                        <Border Height="1" Background="Gray" Margin="0,10,0,10"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBlock x:Name="locationErrorTextBlock" Text="Could not retrieve location." FontSize="16" Foreground="LightGray" Visibility="Collapsed" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="noLocationsTextBlock" Text="No nearby locations found." FontSize="16" Foreground="LightGray" Visibility="Collapsed" HorizontalAlignment="Center"/>
                        <TextBlock Text="Location data provided by © OpenStreetMap contributors and Nominatim." FontSize="12" Margin="0,0,0,0" Foreground="LightGray" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Spacing="10" Margin="0,-40">
            <Button Content="Edit" Click="EditCard_Click" Width="100"/>
            <Button Content="Delete" Click="DeleteCard_Click" Width="100" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Page>
